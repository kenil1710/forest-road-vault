# Absolute URL where this community site is deployed. Used for Open Graph /
# Twitter card image resolution and the "Share on X" link.
# On Vercel this is inferred from NEXT_PUBLIC_VERCEL_URL, but set it explicitly
# for a custom domain so preview and production cards point at the right host.
NEXT_PUBLIC_SITE_URL=https://your-deployment.vercel.app
